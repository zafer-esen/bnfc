Regenerate the `haskell-ci.yml` workflow by
```
make -C ../.. ci
```

Note: Currently the `haskell-ci.yml` has been edited manually. Do not regenerate until extracting to an updated `haskell-ci.diff` file.
